/**
 * Re-export from feature types so paths expected by tooling (e.g. apply worktree) resolve.
 * Source of truth: features/scoring/types/credit_scoring_form.types.ts
 */
export * from "../../types/credit_scoring_form.types";
